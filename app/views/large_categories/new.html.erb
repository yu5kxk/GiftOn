<div class="page-title">カテゴリーリスト</div>

<div class="center-block">
	<div>
		<%= form_for(@large_category) do |f| %>
			<span><%= f.text_field :number, size:'2×10', placeholder: 'No.' %></span>
			<span><%= f.text_field :name, placeholder: '大カテゴリー' %><%= f.submit '追加' %></span>
		<% end %>
		<table>
			<% @large_categories.each do |large| %>
				<tr>
					<th><%= large.number %></th>
					<th><%= large.name %></th>
					<td><%= link_to '編集', edit_large_category_path(large.id) %><%= link_to '削除', large_category_path(large.id), method: :delete %></td>
					<!-- 編集画面は、モーダルかアラート -->
					<td></td>
				</tr>
				<tr>
					<%= form_for(@small_category) do |f| %>
					<td><%= f.hidden_field :large_category_id, :value => large.id %></td>
					<td><%= f.text_field :number, size:'2×10', placeholder: 'No.' %></td>
					<td><%= f.text_field :name, placeholder: '小カテゴリー' %></td>
					<td><%= f.submit '追加' %></td>
					<!-- 編集画面は、モーダルかアラート -->
					<% end %>
				</tr>
				<% large.small_categories.each do |small| %>
					<tr>
						<td></td>
						<td><%= small.number %></td>
						<td><%= small.name %></td>
						<td><%= link_to '編集', edit_small_category_path(small.id) %><%= link_to '削除', small_category_path(small.id), method: :delete %></td>
					</tr>
				<% end %>
			<% end %>
		</table>
	</div>
</div>
